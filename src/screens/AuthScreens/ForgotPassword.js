import {Text, View, SafeAreaView } from "react-native";

const ForgotPassword = (props) => {
    return(
        <View className={`${props.theme === "dark" ? "bg-[#222831]" : "bg-white"} w-full h-full`}>
        </View>
    )
}

export default ForgotPassword;